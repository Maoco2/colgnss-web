name: Keep Render Warm

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend Health
        run: curl -sf -o /dev/null -w "%{http_code}" https://colgnss-web.onrender.com/api/v1/stations
